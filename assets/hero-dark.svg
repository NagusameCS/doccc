<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 200">
  <defs>
    <!-- Dark animated gradient -->
    <linearGradient id="heroGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#1e1b4b">
        <animate attributeName="stop-color" values="#1e1b4b;#312e81;#3730a3;#1e1b4b" dur="8s" repeatCount="indefinite"/>
      </stop>
      <stop offset="50%" style="stop-color:#312e81">
        <animate attributeName="stop-color" values="#312e81;#3730a3;#1e1b4b;#312e81" dur="8s" repeatCount="indefinite"/>
      </stop>
      <stop offset="100%" style="stop-color:#3730a3">
        <animate attributeName="stop-color" values="#3730a3;#1e1b4b;#312e81;#3730a3" dur="8s" repeatCount="indefinite"/>
      </stop>
    </linearGradient>
    
    <!-- Text gradient -->
    <linearGradient id="textGrad" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" style="stop-color:#c7d2fe"/>
      <stop offset="100%" style="stop-color:#a5b4fc"/>
    </linearGradient>
    
    <!-- Version badge gradient -->
    <linearGradient id="versionGrad" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" style="stop-color:#22c55e"/>
      <stop offset="100%" style="stop-color:#16a34a"/>
    </linearGradient>
    
    <!-- Glow filter -->
    <filter id="glow" x="-20%" y="-20%" width="140%" height="140%">
      <feGaussianBlur stdDeviation="4" result="blur"/>
      <feMerge>
        <feMergeNode in="blur"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>
  </defs>
  
  <style>
    @keyframes pulse { 0%, 100% { opacity: 0.2; } 50% { opacity: 0.5; } }
    @keyframes slideIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }
    @keyframes slideInRight { from { opacity: 0; transform: translateX(10px); } to { opacity: 1; transform: translateX(0); } }
    @keyframes dash { to { stroke-dashoffset: 0; } }
    @keyframes versionPulse { 0%, 100% { transform: scale(1); } 50% { transform: scale(1.05); } }
    .particle { animation: pulse 3s ease-in-out infinite; }
    .title { animation: slideIn 0.6s ease-out both; }
    .subtitle { animation: slideIn 0.6s ease-out 0.15s both; opacity: 0; }
    .tagline { animation: slideIn 0.6s ease-out 0.3s both; opacity: 0; }
    .underline { stroke-dasharray: 200; stroke-dashoffset: 200; animation: dash 1s ease-out 0.5s forwards; }
    .version-badge { animation: slideInRight 0.5s ease-out 0.4s both, versionPulse 2s ease-in-out 1s infinite; opacity: 0; }
  </style>
  
  <!-- Background with gradient -->
  <rect width="800" height="200" fill="url(#heroGrad)" rx="16"/>
  
  <!-- Subtle grid pattern -->
  <defs>
    <pattern id="grid" width="40" height="40" patternUnits="userSpaceOnUse">
      <path d="M 40 0 L 0 0 0 40" fill="none" stroke="#a5b4fc" stroke-width="0.3" opacity="0.15"/>
    </pattern>
  </defs>
  <rect width="800" height="200" fill="url(#grid)" rx="16"/>
  
  <!-- Floating particles (subtle) -->
  <circle class="particle" cx="80" cy="35" r="2.5" fill="#a5b4fc"/>
  <circle class="particle" cx="720" cy="85" r="2" fill="#a5b4fc" style="animation-delay: 0.5s;"/>
  <circle class="particle" cx="150" cy="165" r="2" fill="#a5b4fc" style="animation-delay: 1s;"/>
  <circle class="particle" cx="650" cy="155" r="2.5" fill="#a5b4fc" style="animation-delay: 1.5s;"/>
  <circle class="particle" cx="400" cy="20" r="1.5" fill="#a5b4fc" style="animation-delay: 2s;"/>
  <circle class="particle" cx="550" cy="175" r="1.5" fill="#a5b4fc" style="animation-delay: 0.3s;"/>
  
  <!-- Version badge - BOTTOM RIGHT -->
  <g class="version-badge" transform="translate(730, 170)">
    <rect x="-40" y="-12" width="70" height="24" rx="12" fill="url(#versionGrad)"/>
    <text x="-5" y="5" text-anchor="middle" font-family="'SF Mono', Consolas, monospace" font-size="12" fill="white" font-weight="700">v1.0.0</text>
  </g>
  
  <!-- Main title -->
  <text class="title" x="400" y="80" text-anchor="middle" font-family="system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif" font-size="58" font-weight="800" fill="url(#textGrad)" filter="url(#glow)">doccc</text>
  
  <!-- Subtitle -->
  <text class="subtitle" x="400" y="115" text-anchor="middle" font-family="system-ui, -apple-system, sans-serif" font-size="18" fill="#c7d2fe" font-weight="500">README Website Generator</text>
  
  <!-- Tagline with separators -->
  <g class="tagline">
    <text x="400" y="150" text-anchor="middle" font-family="system-ui, -apple-system, sans-serif" font-size="14" fill="#a5b4fc" opacity="0.9">
      <tspan>Compile</tspan>
      <tspan dx="10" opacity="0.4">|</tspan>
      <tspan dx="10">Generate</tspan>
      <tspan dx="10" opacity="0.4">|</tspan>
      <tspan dx="10">Deploy</tspan>
    </text>
  </g>
  
  <!-- Animated underline -->
  <line class="underline" x1="300" y1="170" x2="500" y2="170" stroke="#a5b4fc" stroke-width="2" stroke-linecap="round" opacity="0.5"/>
</svg>
